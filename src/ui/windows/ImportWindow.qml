import QtQuick
import QtQuick.Controls

ApplicationWindow {
    width: 900
    height: 600
    visible: false
    title: qsTr("Import")
    color: "#0a0a0c"
    Label { anchors.centerIn: parent; text: qsTr("Media browser"); color: "#ededef" }
}
